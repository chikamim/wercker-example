box: bradex/aws-lambda-go
build:
  steps:
    #- setup-go-workspace

    # Gets the dependencies
    - script:
        name: go get
        code: |
          cd $WERCKER_SOURCE_DIR
          go version
          go get
          entrypoint
          ls .

